package com.gildedrose.strategy;

import com.gildedrose.Item;

public class SulfurasUpdater implements ItemUpdater {
    @Override
    public void update(Item item) {
        // No need to implement this as Sulfuras doesn't change
    }
}
